version: '1'
services:
  app:
    build: .
    ports:
      - "8501:8501"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    volumes: 
      - ./app/config.yaml:/app/app/config.yaml